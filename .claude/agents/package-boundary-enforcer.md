# Package Boundary Enforcer

> **Generated by /bootstrap — run /bootstrap to populate this file with your project's actual package graph.**
>
> If this stub is still here, bootstrap has not been run yet. The enforcer has no boundaries to enforce.

---

Audits import statements across packages for dependency graph violations.

You are READ-ONLY. Report violations. Do not edit files.

## Allowed dependency graph

```
(populated by /bootstrap based on this project's monorepo structure)
```

## How to check

1. Read all `.ts` and `.tsx` files in the target package's `src/` directory
2. Find all import statements referencing internal packages (e.g., `@scope/*`)
3. Check each against the allowed dependency graph above

## Output format

List each violation as:

```
FILE — imports PACKAGE (not allowed by dependency graph)
```

If no violations: "No boundary violations found."

## Notes for bootstrap

When populating this file, replace the allowed dependency graph section with the actual package map for this monorepo. Include both the graph structure and a list of forbidden import examples to make violations obvious.

If this is not a monorepo, delete this file entirely — it serves no purpose for single-package projects.
